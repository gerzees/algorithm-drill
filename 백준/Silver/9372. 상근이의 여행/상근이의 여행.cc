//20:44,20:52
#include <bits/stdc++.h>

using namespace std;

int T, N, M;
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);

  cin >> T;
  while (T--) {
    cin >> N >> M;
    int a, b;
    while (M--) {
      cin >> a >> b;
    }
    
    cout << N - 1 << '\n';
  }
}