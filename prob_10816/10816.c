#include <stdio.h>

typedef struct _card_
{
    int num;
    int cnt;
} Card;

int main(void)
{
    int N, M;
    int kind_card;
    Card cards[]
    scanf("%d", &N);
    for (size_t i = 0; i < N; i++)
    {
        /* code */
    }
    scanf("%d", &M);
    for (size_t i = 0; i < M; i++)
    {
        /* code */
    }
    
    
    return 0;
}